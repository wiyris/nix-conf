{
  programs.waybar.settings.main.mpris = {
    format = "{dynamic}";
    format-paused = "{status_icon} <i>{dynamic}</i>";
    dynamic-len = 100;
    dynamic-order = [
      "title"
      "artist"
    ];

    dynamic-importance-order = [
      "title"
      "artist"
      "album"
      "position"
      "length"
    ];

    "player-icons" = {
      "default" = " ▶";
      "mpv" = "🎵";
    };

    "status-icons" = {
      # "paused" = "⏸";
    };
  };
}
